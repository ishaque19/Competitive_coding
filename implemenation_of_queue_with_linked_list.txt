Implement push and pop operations of Queue with respect to data structure of Linked List.


//Function to push an element into the queue.
void MyQueue:: push(int x)
{
        // Your Code
        QueueNode *y=new QueueNode(x);
        if(!rear || (rear && !front)){
            front=y;
            rear=y;
        }
        else{
            rear->next=y;
            rear=rear->next;
        }
}

//Function to pop front element from the queue.
int MyQueue :: pop()
{
        // Your Code 
        if(front){
            int x=front->data;
            front=front->next;
            return x;
        }
        else{
            return -1;
        }
}
